# GuanDan4 项目开发规范

> **版本**: 1.1.0
> **最后更新**: 2026-05-04
> **项目阶段**: 阶段 2-3 混合（核心游戏逻辑完成，联网基础就绪）

---

## 1. 项目信息

- **名称**: GuanDan4（掼蛋在线卡牌游戏）
- **框架**: Next.js 16 + React 19 + TypeScript
- **样式**: Tailwind CSS 4 + OKLCH 色彩空间
- **状态管理**: Zustand 5
- **后端**: Supabase（阶段 3）
- **当前进度**: 阶段 2 核心完成（85%），阶段 3 基础设施就绪（60%）

---

## 2. 项目当前依赖

```json
{
  "next": "^16.x",
  "react": "^19.x",
  "react-dom": "^19.x",
  "tailwindcss": "^4.x",
  "typescript": "^5.x",
  "zustand": "^5.x",
  "@supabase/supabase-js": "^2.x"
}
```

**禁止引入的依赖**：
- framer-motion（用 CSS transition 替代）
- three / @react-three/fiber（不使用 3D）
- phaser（回合制卡牌不需要游戏引擎）
- storybook（设计系统在代码中查阅）

---

## 2.1 当前进度

### 已完成 ✅
- 9 个页面全部可访问，所有 UI 组件完整
- 牌组管理、牌型识别、回合逻辑、进贡还贡
- 逆时针出牌顺序（3→2→1→0→3）
- AI 自动出牌（1v3 模式可用）
- 升级规则（calculateUpgrade）
- Zustand 状态管理（game、room）
- Supabase 客户端 + 房间系统 + Realtime
- 结算页接入真实数据
- 112 个单元测试
- OKLCH 色彩系统 + 玻璃态 + 骨架屏 + 动效

### 待完成 📋
| # | 功能 | 优先级 | 对应阶段 |
|---|------|--------|---------|
| 1 | AI 难度系统（easy/medium/hard） | 中 | 阶段 4 |
| 2 | 多局对战（过A最终获胜） | 中 | 阶段 2 |
| 3 | 测试覆盖率达标（>= 90%） | 中 | 阶段 2 |
| 4 | Supabase 联机验收（两浏览器） | 低 | 阶段 3 |
| 5 | PWA / 音效 / 性能优化 | 低 | 阶段 5 |

---

## 3. 核心纪律

### 3.1 零占位符政策

- 不存在 `// TODO: 以后实现` 的函数
- 不存在只声明接口没有实现的文件
- 每个提交的文件都必须完整可用
- 不要为"将来可能需要"写代码——只写当前阶段需要的

### 3.2 垂直切片优先

确保任何时候都有一个可演示的版本。阶段 1 的底线是：所有页面可以浏览，所有交互有 mock 反馈。

### 3.3 渐进式复杂性

每个阶段只引入本阶段必需的依赖。不要在阶段 1 设计阶段 4 才需要的抽象。

---

## 4. 文件组织规范

### 4.1 目录结构

```
src/
├── app/           # Next.js App Router（按路由组织页面）
├── components/    # 组件库
│   ├── ui/        # 基础 UI 组件（通用）
│   ├── game/      # 游戏专用组件
│   └── layout/    # 布局组件
├── lib/           # 逻辑库（纯函数，与UI解耦）
├── styles/        # 全局样式
├── mock/          # Mock 数据（仅阶段 1）
└── types/         # TypeScript 类型定义
```

### 4.2 文件大小

- **每个文件 200-400 行**，上限 800 行
- 超过 400 行就应该拆分为多个文件
- **函数不超过 50 行**
- **嵌套不超过 3 层**

### 4.3 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| React 组件 | PascalCase | `HandArea.tsx` |
| 工具函数 | camelCase | `formatCard.ts` |
| 常量 | UPPER_SNAKE_CASE | `MAX_PLAYERS` |
| 类型/接口 | PascalCase | `Card`, `GameState` |
| Mock 数据 | camelCase + 前缀 | `mockRooms`, `mockHand` |

### 4.4 组件拆分原则

- 页面文件做组装，不做实现
- 复杂 UI 逻辑抽到子组件
- 一个组件只做一件事
- 如果组件的 JSX 超过 150 行，拆分子组件

---

## 4.1 目录结构

```
src/
├── app/           # Next.js App Router（按路由组织页面）
│   ├── layout.tsx           # 根布局
│   ├── page.tsx             # 首页
│   ├── globals.css          # 全局样式 + OKLCH 设计 tokens
│   ├── lobby/page.tsx       # 大厅
│   ├── room/[roomId]/
│   │   ├── page.tsx         # 牌桌主页面
│   │   └── result/page.tsx  # 结算页
│   ├── profile/page.tsx     # 个人资料
│   ├── friends/page.tsx     # 好友
│   ├── leaderboard/page.tsx # 排行榜
│   ├── rules/page.tsx       # 规则
│   └── help/page.tsx        # 帮助
├── components/
│   ├── ui/          # 基础 UI 组件（通用）
│   ├── game/        # 游戏专用组件
│   └── layout/      # 布局组件
├── lib/
│   ├── game/        # 游戏核心逻辑
│   │   ├── types.ts         # Card, HandType, GamePhase 等
│   │   ├── deck.ts          # 牌组管理（108张洗牌发牌）
│   │   ├── rules.ts         # 牌型判断引擎
│   │   ├── turn.ts          # 回合逻辑 + 逆时针 + 升级规则
│   │   ├── tribute.ts       # 进贡/还贡/抗贡
│   │   ├── ai.ts            # AI 决策（单难度）
│   │   └── supabase-bridge.ts # Supabase 持久化桥接
│   ├── store/       # Zustand 状态管理
│   │   ├── game.ts          # 游戏状态（phase/hands/turns/rankings/upgrade）
│   │   └── room.ts          # 房间状态（创建/加入/离开）
│   ├── supabase/    # Supabase 客户端
│   │   ├── client.ts
│   │   ├── auth.ts          # 匿名认证
│   │   ├── rooms.ts         # 房间 CRUD
│   │   └── realtime.ts      # 实时出牌广播
│   └── hooks/
│       └── useRealtime.ts   # Realtime 订阅 hook
├── mock/           # Mock 数据（规则说明等静态内容）
└── types/          # 类型定义
```

---

## 5. 编码规范

### 5.1 TypeScript

- 使用严格模式（`strict: true`）
- **禁止 `any` 类型**
- 使用字面量类型限制取值范围
- 公共函数必须显式声明返回类型
- 所有函数参数必须校验（在系统边界）

### 5.2 React 组件

- 使用函数组件，不使用类组件
- Props 使用 interface 定义，不内联
- 使用 `'use client'` 指令标记客户端组件
- 能用 Server Component 的地方不用 Client Component
- 组件内不直接操作 DOM（使用 React 状态而非 ref）

### 5.3 样式

- 使用 Tailwind CSS 原子类
- 不使用内联 `style={{}}`（除非是动态计算值）
- 自定义 CSS 变量定义在 `globals.css` 的 `:root` 中
- 响应式优先考虑移动端（mobile-first）

### 5.4 错误处理

- UI 层：显示用户友好的错误提示
- 不要静默吞掉异常
- 组件内部错误使用 ErrorBoundary 包裹
- 异步操作必须有 loading 和 error 状态

### 5.5 状态管理

阶段 1 中使用以下方式管理状态：
- **页面级数据**：React `useState` + mock 数据
- **全局 UI 状态**（主题等）：React Context
- **不引入 Zustand**（阶段 2 才会用到）

---

## 6. 分支和提交规范

### 6.1 提交信息格式

```
<type>: <简短描述>
```

类型：`feat`, `fix`, `refactor`, `style`, `docs`, `test`, `chore`

示例：
- `feat: 实现牌桌页面布局和玩家座位组件`
- `feat: 添加卡牌组件（CSS 渲染，支持花色和点数）`
- `fix: 修复手牌区域在移动端的溢出问题`

### 6.2 提交粒度

- 一个提交只做一件事
- 提交信息用中文描述
- 提交前本地验证 `npm run dev` 无错误

---

## 7. 测试规范

### 7.1 当前测试状态

- 测试框架: Vitest
- 测试文件: 7 个（deck, rules, turn, tribute, ai, integration, client）
- 测试数量: 123 个
- 全部通过: ✅

### 7.2 覆盖率要求

| 指标 | 目标 |
|------|------|
| 行覆盖率 | >= 90% |
| 语句覆盖率 | >= 90% |
| 函数覆盖率 | >= 90% |
| 分支覆盖率 | >= 85% |

### 7.3 TDD 流程

1. 先写测试（RED — 测试失败）
2. 写最小实现（GREEN — 测试通过）
3. 重构改进（保持测试绿色）
4. 验证覆盖率达标

---

## 8. 安全要求（阶段 3+）

- 禁止硬编码密钥/令牌/密码
- 所有用户输入在系统边界验证
- 使用 Supabase RLS 策略限制数据访问
- 错误信息不泄露敏感数据

---

## 9. 禁止事项

### 从 GuanDan3 学到的教训

- 不使用 Phaser 或任何游戏引擎（回合制卡牌不需要）
- 不使用 Three.js / R3F（3D 不是 MVP 需求）
- 不引入多代理系统（AI 可以是一个函数）
- 不创建占位符文件
- 不在一个阶段内提前做后面阶段的设计
- 不在组件中直接调用 Supabase（数据访问应封装）
- 不使用超过 5 个 useEffect 在一个组件中（超过 5 个说明需要重构）
- 不引入性能监控/PWA 等基础设施直到核心功能完成
- 不在依赖数组中使用不完整的值

---

## 10. 实施优先级

### 每个阶段的实施顺序：先最难，后最易

阶段 1 的实施顺序：
1. 项目初始化 + Tailwind 配置
2. 设计 tokens（全局 CSS 变量）
3. 基础 UI 组件（Button, Card, Modal, Input, Badge, Avatar, Spinner, Tabs）
4. 布局组件（Navigation, Footer）
5. **牌桌页面**（最复杂，最先攻克）
6. 首页 + 大厅
7. 结算页面
8. 个人资料 + 好友 + 排行榜
9. 规则 + 帮助
10. 响应式适配
11. 用户验收

---

## 11. 自检清单

每次完成任务后确认：
- [ ] `npm run dev` 启动无错误
- [ ] 修改涉及的所有页面可以正常浏览
- [ ] 没有引入阶段外依赖
- [ ] 没有占位符代码
- [ ] 组件文件不超过 400 行
- [ ] 函数不超过 50 行

---

**本文件与 PRD.md 配套使用。PRD 描述"要构建什么"，本文件描述"怎么构建"。**


---




降低大语言模型常见编码错误的行为准则。可根据需要与项目专属规范合并使用。



**取舍说明**：本准则优先兼顾严谨性而非开发速度。处理简单琐碎任务时可灵活酌情使用。



## 1. 编码前先思考

**不要主观臆断，不要掩饰疑惑，直面取舍权衡。**



执行前：

- 明确列出你的所有假设。如有不确定之处，主动询问。

- 若存在多种解读方式，全部列出，不要自行默默选定一种。

- 若有更简便的实现方案，如实提出。必要时敢于提出不同意见。

- 若有内容模糊不清，立即暂停。指明存疑的地方并进行问询。



## 2. 简洁至上

**用最少的代码解决问题，不做任何多余的推测性设计。**

- 不添加需求之外的额外功能。

- 一次性使用的代码不做抽象封装。

- 不设计未被要求的灵活性和可配置性。

- 不为不可能出现的场景做异常处理。

- 若写出200行代码而实际50行即可实现，务必重构精简。



扪心自问：“资深工程师会觉得这样设计过于复杂吗？”如果答案是肯定的，那就进行简化。



## 3. 精准改动原则

**只修改必要部分，只清理自己造成的问题。**



编辑已有代码时：

- 不要擅自“优化”周边代码、注释或代码格式。

- 不要重构没有问题的代码。

- 遵循项目现有的代码风格，即便你习惯用不同写法。

- 若发现无关的冗余废弃代码，只做标注说明，不要擅自删除。



当你的改动产生无效冗余内容时：

- 仅删除因**自身改动**而变得无用的导入项、变量和函数。

- 除非有明确要求，否则不要删除原本就存在的废弃代码。



检验标准：每一行修改过的代码，都应能直接对应业务需求。



## 4. 目标驱动执行

**定义成功标准，循环执行直至验证通过。**



将任务转化为可验证的目标：

- “增加校验功能”→“为非法输入编写测试用例，并使其全部通过”

- “修复漏洞”→“编写可复现该漏洞的测试用例，再修复至测试通过”

- “重构模块X”→“确保重构前后所有测试均可正常通过”



针对多步骤任务，制定简明执行计划：

```

1. [执行步骤] → 验证：[核查标准]

2. [执行步骤] → 验证：[核查标准]

3. [执行步骤] → 验证：[核查标准]

```



清晰明确的成功标准能让你独立推进迭代。模糊的标准（只要求“能用就行”）则需要不断反复沟通确认。



---



**以下情况说明这些准则有效：**代码差异中不必要的改动变少、因过度复杂化导致的返工减少、疑问澄清都在开发实施前完成，而非出错后才补救。

