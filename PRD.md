# GuanDan4 产品需求文档

> **版本**: 1.0.0
> **日期**: 2026-04-28
> **状态**: 待实施

---

## 1. 项目概述

### 1.1 项目背景

GuanDan4 是掼蛋在线卡牌游戏的第二代实现。第一代（GuanDan3）在实施过程中因过程设计缺陷导致项目烂尾。GuanDan4 将从零重建，采用垂直切片策略和过程纪律确保项目成功交付。

### 1.2 项目目标

构建一个**完整可玩的在线掼蛋游戏**，支持：
- 4 人实时对战
- 1v3 AI 练习模式
- 精美的 UI 界面
- 移动端和桌面端适配

### 1.3 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Next.js 16 + React 19 | App Router |
| 语言 | TypeScript 5.x | 严格模式 |
| 样式 | Tailwind CSS 4.x | 原子化 CSS |
| 状态管理 | Zustand 5.x | 阶段 2 引入 |
| 后端 | Supabase | 阶段 3 引入 |
| 测试 | Vitest + Playwright | 阶段 2 引入 |
| 包管理 | pnpm | |

### 1.4 不使用的技术

以下技术在 GuanDan3 中被引入但不应在 GuanDan4 中出现（至少在早期阶段）：

| 技术 | 原因 |
|------|------|
| Phaser | 回合制卡牌游戏不需要游戏引擎 |
| Three.js / R3F | 3D 渲染不是 MVP 需求 |
| framer-motion | 阶段 1 只用 CSS transition |
| Storybook | 设计系统在代码中查阅即可 |
| PWA (next-pwa) | 阶段 5 才考虑 |
| next-pwa | PWA 在阶段 5 才引入 |
| @sentry/nextjs | 监控在联网后才需要 |

---

## 2. GuanDan3 教训总结

### 2.1 根因：水平分层开发

GuanDan3 按技术层级并行推进——UI 组、后端组、AI 组、性能组同时开发。结果每一层都有代码，但没有一层能独立工作。没有垂直切片意味着没有"完整流程"。

### 2.2 具体问题清单

| # | 问题 | 后果 |
|---|------|------|
| 1 | 25+ useEffect 硬桥 React-Phaser 生命周期 | 时序混乱，死锁风险 |
| 2 | 工厂函数返回类定义而非实例 | Phaser Scene 无法初始化 |
| 3 | 事件桥接循环依赖 | React ↔ Phaser 通信断裂 |
| 4 | 大量占位符文件（real-time.ts, uiStore.ts 等） | 给人进度假象，实际不可用 |
| 5 | 游戏状态永远不进入 "playing" | 无法测试任何游戏流程 |
| 6 | AI 和游戏逻辑混在一起 | 难以测试，难以调试 |
| 7 | 过早引入多代理 AI 系统 | 核心游戏还不能玩就建了复杂 AI |
| 8 | 过早引入性能监控/PWA/3D | 增加了复杂度但没有用户价值 |
| 9 | 测试脱离核心流程 | 有 100+ 测试文件但没有验证"4人打完一局" |

### 2.3 核心教训

> **任何时候都应该有一个可玩的版本。哪怕只有一张牌能打出去，也比 100 个页面框架有价值。**

---

## 3. 核心设计原则

### 3.1 垂直切片优先

每个阶段必须产出**一个完整的、可演示的功能切片**，而不是一层技术组件。

```
❌ 错误（GuanDan3）：先做所有 UI → 再做所有逻辑 → 再做所有网络
✅ 正确（GuanDan4）：核心 UI → 本地可玩 → 联网可玩 → AI可玩 → 打磨
```

### 3.2 零占位符政策

提交的代码必须有用。不存在 `// TODO: 以后实现` 的文件。要么完整实现，要么不创建。

### 3.3 先最难、后最易

牌桌 UI 是项目最复杂的组件，阶段 1 应该从牌桌开始，而不是从简单的首页开始。攻克最难问题后再扩展简单页面。

### 3.4 纯函数核心

所有游戏规则都是纯函数。输入参数，返回结果。不依赖数据库、网络、UI。这让规则引擎可以独立测试、独立验证。

### 3.5 渐进式复杂性

每个阶段只引入该阶段必需的依赖。不使用"以防万一将来需要"的库。

---

## 4. 阶段规划

```
阶段 1（UI 壳子）──→ 阶段 2（本地可玩）──→ 阶段 3（联网对战）
                                                  │
                                                  ▼
                              阶段 5（打磨）←── 阶段 4（AI 对手）
```

每个阶段结束都有一个**可演示的里程碑**。只有当前阶段验收通过才能进入下一阶段。

| 阶段 | 里程碑 | 预计工时 |
|------|--------|---------|
| 1 | 所有页面可浏览，交互可点击（mock 数据） | 2周 |
| 2 | 本地单人完整打完一局掼蛋 | 2周 |
| 3 | 4人远程实时对战 | 2周 |
| 4 | 1v3 AI 练习模式 | 1.5周 |
| 5 | 动效/音效/PWA/性能优化 | 1.5周 |

---

## 5. 阶段 1：UI 界面层

### 5.1 目标

完整、精美的静态 UI 界面。所有页面可浏览，所有交互有反馈但使用模拟数据。**没有游戏逻辑，没有网络请求。**

### 5.2 页面清单

#### 5.2.1 首页 `/`
- **Hero 区域**：游戏标题、副标题、两个 CTA 按钮（"开始练习"和"进入大厅"）
- **特色展示**：3 个卡片展示核心特色（经典掼蛋规则、实时对战、智能 AI 对手）
- **简洁页脚**

#### 5.2.2 游戏大厅 `/lobby`
- **房间列表**：卡片网格展示可用房间，每个卡片显示房间名、人数（例如 2/4）、房间类型（练习/对战）
- **搜索栏**：按房间名筛选
- **创建房间按钮**：弹出创建表单（房间名、类型选择）
- **快速加入**：自动分配空位

#### 5.2.3 牌桌页面 `/room/[roomId]`
这是最复杂的页面，需要详细拆解：

**布局结构**（桌面端）：
```
┌──────────────────────────────────────────┐
│  顶栏：房间名 | 局数 | 当前等级 | 设置    │
├──────────┬───────────────────┬────────────┤
│ 对手2    │                   │  对手1     │
│ 头像     │   中央牌桌区域     │  头像      │
│ 手牌数   │   - 最近4轮出牌   │  手牌数    │
│          │   - 当前回合指示   │            │
│          │   - 倒计时        │            │
├──────────┴───────────────────┴────────────┤
│              我的手牌区域                  │
│  [牌] [牌] [牌] [牌] ... [牌] [牌] [牌]   │
│         [出牌] [过牌] [提示]               │
└──────────────────────────────────────────┘
```

**子组件**：
- `TableArea`：中央牌桌。显示 4 个玩家的最近出牌、回合指示、"过"标记
- `PlayerSeat`：玩家座位组件。头像、昵称、剩余手牌数、在线状态、回合高亮
- `HandArea`：手牌区域。卡牌排列、选中状态（抬升+高亮）、牌型分组
- `CardView`：单张卡牌。花色图标 + 点数文字，纯 CSS 渲染
- `ActionBar`：操作栏。出牌/过牌/提示按钮
- `StatusBar`：状态栏。当前回合、倒计时、等级信息

**交互状态**（阶段 1 使用 mock 切换展示）：
- 轮到我出牌（按钮可用、手牌可选中）
- 等待他人出牌（按钮禁用、手牌不可选）
- 手牌为空（胜利展示）
- 对手手牌数为 0（对手已出完）

#### 5.2.4 结算页面 `/room/[roomId]/result`
- **排名展示**：1-4 名，头像、昵称、得分、升级情况
- **对局统计**：总回合数、炸弹数、使用时长
- **操作按钮**：再来一局、返回大厅

#### 5.2.5 个人资料 `/profile`
- **头像 + 昵称**：可编辑
- **战绩概览**：总局数、胜率、连胜记录
- **段位展示**：当前等级图标和名称
- **最近对局**：简化的历史列表

#### 5.2.6 好友页面 `/friends`
- **好友列表**：头像、昵称、在线状态、段位
- **搜索用户**：按昵称搜索
- **好友请求**：待处理的请求

#### 5.2.7 排行榜 `/leaderboard`
- **排名表**：名次、头像、昵称、积分、胜率
- **筛选标签**：全部/周榜/月榜

#### 5.2.8 规则页面 `/rules`
- **规则说明**：基本规则、牌型介绍、大小比较
- **牌型图示**：每种牌型的视觉示例

#### 5.2.9 帮助页面 `/help`
- **FAQ**：常见问题
- **反馈入口**：联系信息

### 5.3 全局组件

#### 5.3.1 Navigation
- Logo + 导航链接（大厅、排行榜、规则、帮助）
- 用户头像下拉菜单（个人资料、好友、设置）
- 移动端：汉堡菜单

#### 5.3.2 设计系统基础组件
阶段 1 需要的基础 UI 组件（全部自己实现，不依赖第三方 UI 库）：

| 组件 | 用途 |
|------|------|
| Button | 主要/次要/危险 变体，多尺寸，loading 状态 |
| Card | 内容容器，可点击变体 |
| Modal | 弹窗对话框 |
| Input | 文本输入，带验证状态 |
| Badge | 标签（在线状态、房间类型） |
| Avatar | 用户头像（支持 fallback 文字） |
| Spinner | 加载指示器 |
| Tabs | 标签页切换 |

### 5.4 Mock 数据设计

```typescript
// src/mock/data.ts

// 示例房间
const mockRooms = [
  { id: 'room-1', name: '新手练习房', type: 'practice', players: 1, maxPlayers: 4, status: 'waiting' },
  { id: 'room-2', name: '高手对决', type: 'battle', players: 3, maxPlayers: 4, status: 'waiting' },
  { id: 'room-3', name: '掼蛋争霸', type: 'battle', players: 4, maxPlayers: 4, status: 'playing' },
];

// 示例玩家
const mockPlayers = [
  { id: 'p1', name: '牌神', avatar: null, seat: 0, cardCount: 27, isOnline: true },
  { id: 'p2', name: '掼蛋高手', avatar: null, seat: 1, cardCount: 20, isOnline: true },
  { id: 'p3', name: '扑克达人', avatar: null, seat: 2, cardCount: 15, isOnline: true },
  { id: 'p4', name: '我', avatar: null, seat: 3, cardCount: 27, isOnline: true, isMe: true },
];

// 示例手牌（27张，按花色点数排序）
const mockHand = [
  { suit: 'spade', rank: 'A', value: 14 },
  { suit: 'heart', rank: 'K', value: 13 },
  // ... 25 more cards
];

// 示例最近出牌（最近4轮）
const mockRecentTurns = [
  { turnNo: 5, seatNo: 0, type: 'play', cards: [{ suit: 'diamond', rank: '5', value: 5 }] },
  { turnNo: 5, seatNo: 1, type: 'pass' },
  { turnNo: 5, seatNo: 2, type: 'pass' },
  { turnNo: 5, seatNo: 3, type: 'play', cards: [{ suit: 'spade', rank: '9', value: 9 }] },
];
```

### 5.5 验收标准

- [ ] 所有 10 个页面可正常访问和浏览
- [ ] 所有按钮/链接有视觉反馈（hover/active/disabled 状态）
- [ ] 牌桌页面展示完整的牌局静态状态（手牌+对手+出牌区+操作栏）
- [ ] 移动端响应式适配（导航变汉堡菜单、牌桌布局调整）
- [ ] Lighthouse 性能评分 > 90
- [ ] 无控制台错误
- [ ] 用户逐页验收通过

---

## 6. 阶段 2：核心游戏逻辑

### 6.1 目标

在阶段 1 的 UI 基础上接入真实游戏规则引擎，实现**本地单人可玩**。一个人可以坐在电脑前，操作 4 个玩家的手牌，按完整掼蛋规则打完一局。

### 6.2 功能需求

#### 6.2.1 牌组管理（deck.ts）
- 两副牌共 108 张（52×2 + 大小王×2）
- 洗牌、发牌（每人 27 张）
- 确定级牌（默认从 2 开始）

#### 6.2.2 牌型判断引擎（rules.ts）
这是整个游戏的核心。必须支持以下牌型：

| 牌型 | 张数 | 说明 |
|------|------|------|
| 单张 | 1 | 任意一张牌 |
| 对子 | 2 | 两张相同点数的牌 |
| 三同张 | 3 | 三张相同点数的牌 |
| 三带二 | 5 | 三同张 + 一个对子 |
| 顺子 | 5 | 五张连续同花色的牌，固定5张，不可含王 |
| 连对 | 6 | 恰好3组连续对子 |
| 钢板 | 6 | 恰好2组连续三同张 |
| 炸弹 | 4-8 | 四张及以上相同点数的牌，张数多者大 |
| 火箭/王炸 | 4 | 四张王牌（两大王+两小王），分值10000 |
| 过 | 0 | 本轮不出牌 |

**卡牌分值**：
- 大王(200) > 小王(100) > 红桃级牌/逢人配(60) > 普通级牌(50) > A(14) > K(13) > ... > 2(2)

**炸弹分值**：
- 王炸(10000) > 级牌炸弹(5000+点数) > 普通炸弹(1000+点数)
- 炸弹之间先比张数（多者大），同张数再比点数
- 炸弹可压制所有非炸弹牌型

**逢人配规则**：
- 红桃级牌可替代除王牌外的任意牌组成牌型，每张替代1张
- 顺子中不可含王牌，级牌仅以原始点数参与计算

#### 6.2.3 回合逻辑（turn.ts）
- 非进贡局随机先出；进贡局由头游先出
- 领牌者必须出牌，不能Pass
- 连续 3 人过牌则最后出牌者获得新一轮领牌权
- 出牌合法性校验（牌型匹配 + 点数大于上家）
- 手牌清空判定（先出完者获胜）

#### 6.2.4 进贡/还贡逻辑（tribute.ts）
- 末游向头游进贡手中最大的牌（不得小于10），头游还贡（必须小于10）
- 头游和二游为同队时，末游向头游进贡，同时三游向二游进贡
- 抗贡条件：持有双红大王（两张红桃大王）

#### 6.2.5 升级规则
- 头游+二游（同队）升3级 | 头游+三游升2级 | 头游+末游升1级
- 级牌从2开始，过A且获头游为最终获胜

### 6.3 状态管理

```typescript
interface GameState {
  // 牌局状态
  status: 'idle' | 'dealing' | 'playing' | 'tribute' | 'finished';
  phase: 'deal' | 'tribute' | 'play' | 'result';

  // 牌组
  deck: Card[];
  hands: Card[][];  // hands[0-3] -> 每个玩家的手牌

  // 回合
  currentSeat: number;  // 当前该谁出牌
  turnNo: number;
  lastPlay: { seatNo: number; type: HandType; cards: Card[] } | null;
  passCount: number;  // 连续过牌数，3次则重新领牌
  recentTurns: TurnRecord[];  // 最近4轮出牌记录

  // 级牌
  levelRank: number;  // 当前级牌点数（2,3,...,A）

  // 结算
  rankings: number[];  // 出完顺序 [seatNo, ...]
  scores: number[];    // 各玩家得分

  // 进贡
  tributeFrom: number[];
  tributeTo: number[];
  tributeCards: Card[];
  returnCards: Card[];
}
```

### 6.4 本地单人模式

阶段 2 的"本地单人"含义：屏幕显示当前玩家的手牌，但可以通过 Tab 键或按钮切换到其他玩家的视角，手动替每个玩家出牌。这样一个人可以测试完整对局。

或者更简单的方案：在开发阶段，把 4 个玩家都设置为"人类"，程序不区分 AI/真人，所有座位都需要手动操作。

### 6.5 验收标准

- [ ] 完整的牌型判断引擎通过单元测试
- [ ] 发牌、出牌、过牌流程完整可走通
- [ ] 炸弹/火箭正确判断
- [ ] 进贡/还贡流程正确
- [ ] 升级规则正确
- [ ] 可以手动操作 4 个座位完成一整局
- [ ] 单元测试覆盖率 >= 90%
- [ ] 至少一个集成测试：完整打一局并验证结果

---

## 7. 阶段 3：联网对战

### 7.1 目标

接入 Supabase 后端，实现多人实时对战。

### 7.2 功能需求

- **匿名认证**：用户无需注册，自动生成匿名身份
- **房间系统**：创建/加入/离开房间，房间状态同步
- **实时出牌**：通过 Supabase Realtime 广播出牌事件
- **断线重连**：心跳检测，断线后自动恢复状态
- **RLS 安全策略**：确保用户只能操作自己的手牌

### 7.3 数据库表设计

```sql
-- 房间表
rooms (id, name, type, status, level_rank, created_at)
-- 房间成员
room_members (id, room_id, user_id, seat_no, is_ready, is_online, last_heartbeat)
-- 对局表
games (id, room_id, status, state_public, state_private, created_at)
-- 出牌记录
turns (id, game_id, turn_no, seat_no, action_type, payload, created_at)
```

### 7.4 验收标准

- [ ] 两个不同浏览器可以进入同一个房间并开始对局
- [ ] 出牌实时同步，延迟 < 500ms
- [ ] 断线后 30 秒内可以重连并恢复状态
- [ ] 并发 20 人压力测试通过

---

## 8. 阶段 4：AI 对手

### 8.1 目标

实现 AI 对手，支持 1v3 练习模式（1 个真人 + 3 个 AI）。

### 8.2 AI 设计

**简化原则**：不使用多代理系统。AI 就是一个函数：

```typescript
function aiDecide(
  hand: Card[],
  lastPlay: LastPlay | null,
  levelRank: number,
  difficulty: 'easy' | 'medium' | 'hard'
): { type: 'play'; cards: Card[] } | { type: 'pass' }
```

**难度差异**：
- 简单：随机出合法牌型，30% 概率过牌
- 中等：选最大的可出牌型，考虑保留炸弹
- 困难：分析牌局，优化出牌策略

### 8.3 验收标准

- [ ] 1 真人 + 3 AI 可以完成一局
- [ ] AI 出牌在 1 秒内完成
- [ ] 三个难度有明显行为差异
- [ ] 困难 AI 对简单 AI 胜率 > 70%

---

## 9. 阶段 5：深化设计与打磨

### 9.1 目标

在完整可玩的基础上增加视觉和体验层面的优化。

### 9.2 功能需求

- **卡牌动画**：发牌动画、出牌飞行动画、翻牌动画（用 CSS transition，不引入重型动效库）
- **音效**：出牌音效、炸弹音效、胜利音效
- **PWA**：离线提示、安装引导
- **性能优化**：懒加载、代码分割、图片优化
- **无障碍**：键盘导航、屏幕阅读器支持

### 9.3 验收标准

- [ ] Lighthouse 性能评分 > 90（含动画）
- [ ] 移动端触控操作流畅
- [ ] PWA 可安装，离线有提示
- [ ] 键盘可完成出牌操作

---

## 10. 项目目录结构

```
GuanDan4/
├── PRD.md                       # 本文档
├── CLAUDE.md                    # AI 开发规范
├── package.json
├── tsconfig.json
├── next.config.ts
├── tailwind.config.ts
├── postcss.config.mjs
├── vitest.config.ts             # 阶段 2 加入
├── src/
│   ├── app/
│   │   ├── layout.tsx           # 根布局（全局样式、导航）
│   │   ├── page.tsx             # 首页
│   │   ├── globals.css          # 全局样式 + 设计 tokens
│   │   ├── lobby/
│   │   │   └── page.tsx         # 大厅
│   │   ├── room/
│   │   │   └── [roomId]/
│   │   │       ├── page.tsx     # 牌桌主页面
│   │   │       └── result/
│   │   │           └── page.tsx # 结算页
│   │   ├── profile/
│   │   │   └── page.tsx
│   │   ├── friends/
│   │   │   └── page.tsx
│   │   ├── leaderboard/
│   │   │   └── page.tsx
│   │   ├── rules/
│   │   │   └── page.tsx
│   │   └── help/
│   │       └── page.tsx
│   ├── components/
│   │   ├── ui/                  # 基础 UI 组件
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Badge.tsx
│   │   │   ├── Avatar.tsx
│   │   │   ├── Spinner.tsx
│   │   │   └── Tabs.tsx
│   │   ├── game/                # 游戏专用组件
│   │   │   ├── PlayingCard.tsx  # 单张卡牌
│   │   │   ├── HandArea.tsx     # 手牌区域
│   │   │   ├── TableArea.tsx    # 中央牌桌
│   │   │   ├── PlayerSeat.tsx   # 玩家座位
│   │   │   ├── ActionBar.tsx    # 操作栏
│   │   │   └── GameStatusBar.tsx
│   │   └── layout/
│   │       ├── Navigation.tsx
│   │       └── Footer.tsx
│   ├── lib/
│   │   ├── game/                # 游戏核心逻辑（阶段 2）
│   │   │   ├── rules.ts         # 牌型判断
│   │   │   ├── rules.test.ts
│   │   │   ├── deck.ts          # 牌组管理
│   │   │   ├── deck.test.ts
│   │   │   ├── turn.ts          # 回合逻辑
│   │   │   ├── turn.test.ts
│   │   │   ├── tribute.ts       # 进贡逻辑
│   │   │   └── tribute.test.ts
│   │   ├── store/               # 状态管理（阶段 2）
│   │   │   └── game.ts
│   │   ├── supabase/            # Supabase 客户端（阶段 3）
│   │   │   └── client.ts
│   │   └── types/               # 类型定义
│   │       └── game.ts          # Card, HandType, GameState 等
│   └── mock/                    # Mock 数据（阶段 1）
│       └── data.ts
├── public/
│   └── ...                      # 静态资源
└── tests/                       # E2E 测试（阶段 3+）
    └── ...
```

---

## 11. 过程纪律

### 11.1 零占位符政策

- 不存在只声明接口不实现的文件
- 不存在 `// TODO: 以后实现` 的函数体
- 每个文件要么完整，要么不创建

### 11.2 垂直切片验收

每个阶段结束时必须满足：
- `npm run dev` 启动后能完整演示阶段目标
- 不是"代码写完了"，是"跑给你看能工作"
- 核心用户路径无阻断

### 11.3 渐进式复杂性

- 每个阶段只引入该阶段必需的 npm 依赖
- 不为"将来可能需要"而提前引入
- 阶段 1 的依赖：next, react, react-dom, tailwindcss, typescript, @types/react, @types/react-dom
- 阶段 2 加入：zustand, vitest, @testing-library/react
- 阶段 3 加入：@supabase/supabase-js
- 阶段 4 加入：无额外依赖
- 阶段 5 加入：按需

### 11.4 代码审查清单

每次提交前检查：
- [ ] 这道门通过后能否正常演示？
- [ ] 有没有引入不必要的依赖？
- [ ] 有没有占位符代码？
- [ ] 错误是否被静默吞掉？
- [ ] 文件是否在合理大小范围内（< 400 行）？

### 11.5 防烂尾底线

阶段 2 完成后，项目就有一个**永不回退的底线**——一个可以本地打完整局的掼蛋游戏。之后任何阶段的失败都不会让项目归零。

---

## 12. 术语表

| 中文 | 英文 | 定义 |
|------|------|------|
| 掼蛋 | GuanDan | 四人两副牌扑克游戏 |
| 级牌 | Level Card | 当前等级的牌，红桃级牌为万能牌（逢人配） |
| 逢人配 | Wild Card | 红桃级牌，可替代任意牌组成牌型 |
| 进贡 | Tribute | 末游向头游进献最大牌（不得小于10） |
| 还贡 | Return | 头游返还一张牌给进贡者 |
| 抗贡 | Resist | 持有双大王可拒绝进贡 |
| 头游 | First | 最先出完手牌 |
| 末游 | Last | 最后出完手牌 |
| 炸弹 | Bomb | 四张及以上相同点数的牌 |
| 火箭 | Rocket | 四张王牌（两大王+两小王），最大的牌型 |
| 顺子 | Straight | 五张同花色连续点数的牌 |
| 三连对 | Sequence Pairs | 三组连续的对子 |
| 钢板 | Sequence Triples | 二连三，两组连续的三同张 |
| 领牌 | Lead | 新一轮首先出牌 |
| 跟牌 | Follow | 按领牌牌型压过 |
| 过牌 | Pass | 放弃本轮出牌 |
